services:
  project:
  postgres:
    build:
      context: ./db
    container_name: postgres
    restart: always
    env_file:
          - .env
    logging:
        options:
            max-size: 3m
            max-file: "3"
    ports:
    - 5433:5432
    networks:
      - bridge