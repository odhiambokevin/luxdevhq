# Gas Prices Pipeline

A simple pipeline that extracts data from an api, converts it to json. The data is the transformed using pandas and loaded to a database.

Run the `main.py` script to automate all the other processes.